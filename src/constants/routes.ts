/* eslint-disable no-unused-vars */
export enum Routes {
    home = '/',
    register = '/sign-up',
    chat = '/messenger',
    profile = '/settings',
    serverError = '/500',
    notFoundError = '/not_found',
}
