/* eslint-disable no-unused-vars */
export enum Routes {
    home = '/',
    register = '/register',
    chat = '/chat',
    profile = '/profile',
    serverError = '/500',
    notFoundError = '/not_found',
}
